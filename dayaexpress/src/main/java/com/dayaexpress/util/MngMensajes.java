package com.dayaexpress.util;

public class MngMensajes {
    public static void showInfo(String titulo, String mensaje){
        //mostrarMensaje(titulo, mensaje, FacesMessage.SEVERITY_INFO);
    }
    
    public static void showWarn(String titulo, String mensaje){
       // mostrarMensaje(titulo, mensaje, FacesMessage.SEVERITY_WARN);
    }
    
    public static void showError(String titulo, String mensaje){
       // mostrarMensaje(titulo, mensaje, FacesMessage.SEVERITY_ERROR);
    }
    
    public static void showFatal(String titulo, String mensaje){        
        //mostrarMensaje(titulo, mensaje, FacesMessage.SEVERITY_FATAL);
    }
    
    private static void mostrarMensaje(String titulo, String mensaje){ 
       //     FacesMessage.Severity severity){
        //FacesContext context = FacesContext.getCurrentInstance();
       // FacesMessage message = new FacesMessage();
       // message.setSeverity(severity);
       // message.setSummary(titulo);
      //  message.setDetail(mensaje);
       // context.addMessage(null, message);
    }
}
